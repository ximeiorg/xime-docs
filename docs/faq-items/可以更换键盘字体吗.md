---
order: 14
category: 个性化
q: 可以更换键盘字体吗？
linkHref: /features/keyboard-config
linkText: 字体配置说明
---

可以（v2.8.0+）。在「输入方案 → 浏览器导入」中上传 .ttf / .otf / .woff / .woff2 字体文件（自动保存到 rime/fonts/），然后在 xime.custom.yaml 的 keyboard.fonts 中分别指定按键、字根、候选等字体，重新部署生效。
